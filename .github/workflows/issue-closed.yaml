# synced from apix-actions/.github/workflows/issue-closed.yaml
name: Close Jira Ticket on Issue Close

on:
  issues:
    types: [closed]

permissions: {}

jobs:
  close-jira:
    permissions:
      contents: read
      issues: read
    uses: mongodb/apix-action/.github/workflows/_issue-closed.yaml@ef5a3322db780b664092ae80ac06b710ec1bb1de # main
    secrets: inherit